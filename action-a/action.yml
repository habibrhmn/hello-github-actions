name: "Hello Actions"
description: "Greet someone"
author: "octocat@github.com"

inputs:
   WORLD:
    description: "What to do"
    required: true
    default: "World"

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "mic"
  color: "purple"
